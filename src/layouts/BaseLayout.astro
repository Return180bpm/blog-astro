---
import Header from '@/components/Header.astro'
import Footer from '@/components/Footer.astro'
import '../styles/global.css'

interface Props {
	title: string
	description?: string
}

const { description, title }: Props = Astro.props
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<title>{title}</title>
		{description && <meta name="description" content={description} />}
	</head>
	<body
		class="grid place-content-center gap-12 font-sans text-stone-600 bg-light"
	>
		<Header />
		<main
			class="overflow-x-hidden px-4 prose-custom prose-md lg:prose-xl md:prose-lg prose-stone"
		>
			<slot />
		</main>
		<Footer />
	</body>
</html>
